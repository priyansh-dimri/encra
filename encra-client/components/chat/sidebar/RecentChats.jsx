const RecentChats = () => {
  // Displays a list of recent conversations (names + usernames)
  // For unread messages, we show a small icon instead of a count
  return (
    <div style={{ flex: 1, overflowY: "auto" }}>
      {/* Each item: username (bold), name (smaller), unread dot/icon on right */}
    </div>
  );
};

export default RecentChats;
